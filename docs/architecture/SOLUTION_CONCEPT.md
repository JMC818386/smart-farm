# SOLUTION CONCEPT — PHASE I

Frontend:
- React + Vite + TypeScript
- Zustand state management
- Axios API client

Backend:
- Node + Express
- Static JSON (mock)

Data Flow:
UI → Zustand → Axios → Express → JSON → UI

All APIs prefixed with /api/*
